PC-BASIC font building scripts
==============================

The scripts under this directory can be used to build the .hex font files supplied with PC-BASIC.

The script requires `monobit==v0.13` and the `upx` extractor.
